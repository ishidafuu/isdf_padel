---
description: GitHub Issue の状況を確認
argument-hint: [--open | --mine | --milestone <name>]
---

# /gh-sync コマンド

GitHub Issue の状況を確認します。

**オプション**: $ARGUMENTS

## 使用者

**🧑 人間専用コマンド** - GitHub Issue の状況を把握する際に使用

エージェントはこのコマンドを使用しません。

## 使用方法

```
/gh-sync                    # オープン中の Issue 一覧
/gh-sync --mine             # 自分にアサインされた Issue
/gh-sync --milestone v0.1   # マイルストーン別
```

## オプション解析

`$ARGUMENTS` から以下を解析：
- `--open`（デフォルト）: オープン中の Issue 一覧
- `--mine`: 自分にアサインされた Issue
- `--milestone <name>`: 指定マイルストーンの Issue

## 指示

GitHub CLI を使用して Issue の状況を表示してください。

### オプション別の実行

| オプション | 説明 | コマンド |
|-----------|------|---------|
| (なし) | オープン中の Issue 一覧 | `gh issue list --state open --limit 20` |
| --mine | 自分にアサインされた Issue | `gh issue list --assignee @me --state open` |
| --milestone <n> | 指定マイルストーンの Issue | `gh issue list --milestone "<n>"` |

## 出力形式

```
=== GitHub Issues ===

## Open Issues (5件)

| # | タイトル | ラベル | 担当 |
|---|---------|--------|------|
| #12 | [30101] ジャンプ機能実装 | impl, feat/player | user |
| #13 | [30102] 移動機能実装 | impl, feat/player | - |
| #14 | [30201] スライム敵実装 | impl, feat/enemy | user |
| #15 | [20005] イベントシステム設計 | design | - |
| #16 | バグ: ジャンプ高さ不正 | bug | - |
```

**補足**: Issue タイトルの `[番号]` から仕様書を特定可能（例: `[30101]` → `30101_*_spec.md`）

## GitHub CLI コマンド

```bash
# オープン中の Issue
gh issue list --state open --limit 20

# 自分の Issue
gh issue list --assignee @me --state open

# マイルストーン別
gh issue list --milestone "v0.1"

# 特定ラベル
gh issue list --label "impl"
```

## 注意事項

- GitHub CLI (`gh`) がインストールされ、認証済みである必要がある
- リポジトリが GitHub にプッシュされている必要がある

## 終了コード

| コード | 意味 |
|--------|------|
| 0 | 正常終了 |
| 1 | GitHub CLI エラー（未認証、リポジトリなし等） |

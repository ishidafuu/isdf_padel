---
id: "R30000-006"
title: "未使用コードの削除"
type: "refactor"
status: "todo"
priority: "low"
related_task: null
spec_ids: []
blocked_by: []
blocks: []
branch_name: null
worktree_path: null
plan_file: null
tags: ["refactor", "code-quality", "dead-code", "cleanup", "audit"]
parent_task_id: null
audit_source: "2026-01-08"
severity: "minor"
category: "code-quality"
created_at: "2026-01-08T15:02:53+09:00"
updated_at: "2026-01-08T15:02:53+09:00"
completed_at: null
---

# Task R30000-006: 未使用コードの削除

## Summary

コード監査で検出された未使用の関数・メソッド・enum バリアントを削除し、コードベースをクリーンに保つ。

## 検出された問題

### 未使用メソッド（5件）

| ファイル | 行 | アイテム |
|----------|-----|---------|
| src/components/mod.rs | 17 | `fn new()` (never used) |
| src/components/mod.rs | 100 | `fn new()`, `fn is_moving()` (never used) |
| src/components/mod.rs | 137 | `fn reset()` (never used) |
| src/components/mod.rs | 414 | `fn new()` (never used) |
| src/resource/scoring.rs | 154 | `fn check_game_win()` (never used) |

### 未使用 enum バリアント（2件）

| ファイル | 行 | アイテム |
|----------|-----|---------|
| src/resource/scoring.rs | 107 | `GameWon` variant (never constructed) |
| src/resource/scoring.rs | 107 | `SetWon` variant (never constructed) |

**深刻度**: MINOR（機能に影響なし、警告のみ）

## 修正方針

### 1. 削除対象の確認

各アイテムが本当に不要か確認：
- 将来使用予定があるか
- ドキュメント/テスト目的か
- 単純に削除可能か

### 2. 削除方法

```rust
// 単純に該当行を削除
// または #[allow(dead_code)] を付与（将来使用予定の場合）
```

### 3. 検証

```bash
cargo check 2>&1 | grep -c "warning.*never used"
# 0 になることを確認
```

## Progress

### Completed

（未着手）

## Next Actions

1. `src/components/mod.rs` の未使用メソッドを確認・削除
2. `src/resource/scoring.rs` の未使用メソッド・バリアントを確認・削除
3. `cargo check` で警告が解消されたことを確認

## 完了チェックリスト

> このタスクは in-review 経由必須

- [ ] ビルド成功（`cargo build`）
- [ ] 未使用コード警告が 0 件
- [ ] 将来使用予定のコードには `#[allow(dead_code)]` + コメント追加
- [ ] in-review に移動済み
- [ ] レビュー完了

## メモ

- 監査で検出: 2026-01-08
- enum バリアントは将来のスコアリング機能で使用予定の可能性あり
  - その場合は `#[allow(dead_code)]` + TODO コメントを追加
- `check_game_win()` は試合勝利判定で将来使用予定かもしれない
